import { D5nDirective } from './d5n.directive';

describe('D5nDirective', () => {
  it('should create an instance', () => {
    const directive = new D5nDirective();
    expect(directive).toBeTruthy();
  });
});
